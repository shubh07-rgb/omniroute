# Copy this to .env (or export directly) on the VM.
# No key file needed — GCS auth uses the VM's attached service account.

GCS_BUCKET_NAME=your-bucket-name-here

KAFKA_BOOTSTRAP_SERVERS=10.142.0.2:9092
KAFKA_TOPIC=vehicle-telemetry
